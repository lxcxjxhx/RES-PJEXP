# feat(S-12): 论文输入 tex 源优先 + PDF 多级降级链，输出升级为美观 HTML/PDF

**PR 链接**: [lxcxjxhx/HOS_SKILL_WORKFLOW#47](https://github.com/lxcxjxhx/HOS_SKILL_WORKFLOW/pull/47)
**状态**: Merged
**合并时间**: 2026-08-02T14:16:11Z
**创建时间**: 2026-08-02T14:04:36Z
**作者**: [lxcxjxhx](https://github.com/lxcxjxhx)

## 统计信息

- **新增行数**: 1362
- **删除行数**: 31
- **变更文件数**: 10
- **提交数**: 1

## 变更文件

| 文件 | 状态 | 新增 | 删除 |
|------|------|------|------|
| `S-12-HOS-Critic-Review/CHANGELOG.md` | MOD | +28 | -0 |
| `S-12-HOS-Critic-Review/README.md` | MOD | +9 | -1 |
| `S-12-HOS-Critic-Review/SKILL.md` | MOD | +10 | -2 |
| `S-12-HOS-Critic-Review/docs/09-extraction-and-rendering.md` | NEW | +79 | -0 |
| `S-12-HOS-Critic-Review/scripts/cli.ts` | MOD | +63 | -3 |
| `S-12-HOS-Critic-Review/scripts/render-html.ts` | NEW | +239 | -0 |
| `S-12-HOS-Critic-Review/scripts/tools/pdf-extract.py` | MOD | +419 | -25 |
| `S-12-HOS-Critic-Review/scripts/tools/render-pdf.py` | NEW | +156 | -0 |
| `S-12-HOS-Critic-Review/scripts/tools/tex-fetch.py` | NEW | +298 | -0 |
| `S-12-HOS-Critic-Review/tests/unit/render-html.test.ts` | NEW | +61 | -0 |

## PR 描述

## 背景与动机

对 8 篇 arxiv 论文实测后发现的真实问题（本次 PR 的直接证据）：

1. **PDF 直提丢内容/拆碎片**：`pdf-extract.py` v1 只用 `page.get_text()`。
   - 公式被拆成碎片：`k ∈`、`j ∈ES`、下标丢失（`vso` 变 `vso`）
   - 表格提取后列错乱 → **会直接污染评审结论**（如某篇报告中"表格多处 Loc>Det 违反指标单调性"实际是表格列错乱造成的误判）
   - 扫描/图密集页静默丢内容：T2L-Agent 共 41 页，**第 21-41 页全部为图表页**，v1 静默缺一半内容却无任何提示
2. **输出只有 MD**：`render.ts` 只出 Markdown，格式在部分场景变形；用户需要美观可浏览的 web 页面 / PDF。
3. **PDF 转换消耗 token**：若让宿主 LLM 自行把 MD 转成 PDF，昂贵且不可控——应交给 script 完成。

## 变更清单

### 输入侧：tex 源优先 + PDF 多级降级链

- **新增 `scripts/tools/tex-fetch.py`**：arXiv id/URL → e-print → 主 `.tex` → 纯文本。
  公式保留 LaTeX 原文（`[FORMULA: Attention(Q,K,V)=softmax(QKᵀ/√d_k)]`，LLM 可读），
  `\input/\include` 递归展开，表格/图保留 caption 占位。实测 `1706.03762` 成功。
- **重写 `scripts/tools/pdf-extract.py` v2**：
  - `structured`：text block 重建阅读顺序 + 双栏/单栏自动检测重排
  - 表格 `find_tables()` → Markdown 表格块
  - 数学字体 span / 孤立符号行 → `[FORMULA]` 占位
  - `docx` 模式：PDF → docx（libreoffice）→ gfm（pandoc）
  - `ocr` 模式：`HOS_OCR_API`（OpenAI 兼容视觉）→ 本地 tesseract 兜底
  - **quality 自检**：`layout / char_per_page / scanned_pages / formula_count / table_count / warnings`
    写入输出，宿主据此在报告中如实写 degradations（降级不撒谎）
  - 修复 PyMuPDF 1.27 stdout layout 提示污染 JSON 管道

### 输出侧：MD → HTML → PDF（零额外 LLM token）

- **新增 `scripts/render-html.ts`**：ReviewReport JSON → 单文件美观 HTML。
  内联 CSS：评分卡大卡片、六维进度条、severity 彩色徽章、critique 分组、`@media print` A4 样式、
  **HTML 转义防注入**（finding/claim 文本来自 LLM，必须转义）。
- **新增 `scripts/tools/render-pdf.py`**：HTML → PDF 降级链
  `weasyprint → playwright(chromium) → msedge headless → chrome headless`，失败给清晰安装提示。
  本机实测 weasyprint 缺库 → 自动降级 Edge headless 出 4 页 PDF。
- **`scripts/cli.ts`**：`render --format md|html|pdf|auto`（按 `--out` 后缀推断，pdf 自动先 HTML 再转）；
  新增 `extract pdf` 子命令；stdout JSON 容错解析。

### 文档与测试

- `README.md` / `SKILL.md` / `CHANGELOG.md`（0.4.0）+ 新增 `docs/09-extraction-and-rendering.md`
- 新增 `tests/unit/render-html.test.ts`（5 个：结构/评分卡/quick 裁剪/防注入/徽章），全套 54 通过

## 验证

- 5 篇真实论文 v2 提取：layout/表格/公式/扫描页自检全部生效（T2L-Agent 21-41 页低密度被显式暴露）
- tex-fetch 实测 arXiv 1706.03762：10 源文件展开、公式 LaTeX 保留
- `cli extract pdf` + `cli render --format html/pdf` 端到端通过
- `npm test`：54/54 通过

## 兼容性

- v1 的 `--mode text` 与 `page.get_text()` 路径保留（兜底）
- CLI 旧命令（fetch/chunk/run/validate/store）签名不变

## 相关链接

- **源仓库**: [lxcxjxhx/HOS_SKILL_WORKFLOW](https://github.com/lxcxjxhx/HOS_SKILL_WORKFLOW)
- **PR 链接**: https://github.com/lxcxjxhx/HOS_SKILL_WORKFLOW/pull/47
- **Diff**: https://github.com/lxcxjxhx/HOS_SKILL_WORKFLOW/pull/47.diff

---
*Auto-generated at 2026-08-08 01:18:51 UTC*
